import React from 'react';
import { LaunchFilter } from '../LaunchFilter/LaunchFilter';
import { Slider } from '../Slider';

export const Home = () => (
  <div>
    <Slider />
    <LaunchFilter />
  </div>
);
